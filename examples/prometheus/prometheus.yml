global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/prometheus-template-exporter.yml

scrape_configs:
  - job_name: template-exporter
    static_configs:
      - targets:
          - exporter:9900
